# 改动记录

> **版本号规矩**：每改一次 **+0.0.1**；末位满 10 **进位**。
> 现在是 **1.2.0**（从 1.0.0 起，共 20 次改动）。

## 1.2.0
- **复述被拦时会说出来了**。有些内容 DeepSeek 不肯原样重复，它会改口回一句
  「你好，这个问题我暂时无法回答，让我们换个话题再聊聊吧。」——
  以前这种情况**看不出来**：照样去点朗读，状态还写「念完了」，念出来的是那句拒绝。
  现在会认出它，卡片上写**「没成：被拦了」**，不点朗读了。
- ⚠ 判据**两头看**：回复里有、而**发过去的原文里没有**，才算被拦 ——
  不然正文里本来写着「换个话题」（小说对白）会被误报。

## 1.1.9
- 补上 `repository` 字段（指向本仓库）
  - 装上之后，插件市场靠这个认「装的到底是哪一个」
  - 只动 `package.json`，没动代码

## 1.1.8
- 修：换屏（竖屏↔横屏）、浏览器放大之后，卡片可能跑到窗口外面找不见
  - 落位时把坐标夹进视口；拖动也夹着，拖不出屏幕
  - 转屏 / 缩放自动重摆（`resize`、`orientationchange`、`visualViewport`）
  - **右键卡片 = 回到右下角**；被夹回视野时会闪一下绿框

## 1.1.7
- 「听谁」「收藏」展开时箭头会翻转（▾ → ▴）
- 版本号规范 + 这份改动记录

## 1.1.6
- 分清三种状态：没装扩展 / 装了但页面没开 / 正常

## 1.1.5
- 卡片显示扩展文件夹在你这台电脑上的真实路径（点一下就能复制）
- 路径点一下就复制；并把 extension 文件夹也放进已装的包里

## 1.1.4
- 补上 extension 文件夹的位置说明，并把它加进 npm files

## 1.1.3
- 修：改了会话名字，听谁清单不跟着变

## 1.1.2
- 听谁清单：没起名字的空会话不列出来

## 1.1.1
- 修：链接被拖动逻辑吃掉了点击；顺便让地址能复制

## 1.1.0   ← 第 10 次改动（末位满 10，进位）
- 没装扩展时，卡片直接把仓库地址摆出来（可点）

## 1.0.9
- 实测：不需要改浏览器设置，第一次自己点一下朗读就行（Edge 也没那个设置项）

## 1.0.8
- 宿主没更新时不乱提示「扩展没连上」

## 1.0.7
- 没装浏览器扩展时，卡片直接提示「还差一步」

## 1.0.6
- 两个 DSH 同时开着时，谁有活儿就干谁的（之前只认 3080）

## 1.0.5
- 扩展同时支持网页版(3080)和桌面版(43129)

## 1.0.4
- 截图换成更清楚的那张，顺便改成更好认的文件名

## 1.0.3
- 换一张更清楚的扩展安装截图

## 1.0.2
- 加上两张截图（装扩展、小卡片）

## 1.0.1
- 补上 .gitignore 等收尾

## 1.0.0
- 让 DSH 里的会话用 DeepSeek 网页端的声音念出来